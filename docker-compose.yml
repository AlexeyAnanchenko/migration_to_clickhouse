version: "3.8"
services:
  postgres:
    image: postgres:latest
    ports:
      - 5432:5432
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: postgres_db
    volumes:
      - ./postgres/ddl/init.sql:/docker-entrypoint-initdb.d/init.sql
      - ./postgres/dml/load_data.sql:/docker-entrypoint-initdb.d/load_data.sql
      - ./postgres/dml/query_for_data_mart.sql:/docker-entrypoint-initdb.d/query_for_data_mart.sql
    healthcheck:
      test: ["CMD", "pg_isready", "-U", "postgres"]
      interval: 5s
      retries: 5
    restart: always

  clickhouse:
    image: clickhouse/clickhouse-server:latest
    networks:
        - clickhouse
    depends_on:
      - postgres
    ulimits:
        nofile:
          soft: 262144
          hard: 262144
    ports:
      - "8123:8123"
    volumes:
      - "clickhouse-data:/var/lib/clickhouse"

  click-ui:
    image: spoonest/clickhouse-tabix-web-client
    networks:
        - clickhouse
    depends_on:
      - clickhouse
    ports:
      - "8124:80"
    restart: always
volumes:
  clickhouse-data:
networks:
    clickhouse: